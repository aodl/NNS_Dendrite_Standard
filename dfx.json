{
  "version": 1,
  "canisters": {
    "dendrite": {
      "type": "custom",
      "candid": "canisters/dendrite/dendrite.did",
      "wasm": "target/wasm32-unknown-unknown/release/dendrite.wasm",
      "build": "npm run build && cargo build --release --target wasm32-unknown-unknown -p dendrite"
    }
  },
  "defaults": { "build": { "packtool": "" } }
}

